# YouTube_parse_python_bot: бот парсит новые видео с youtube

> Это инструмент получения уведомлений в виде постов о вышедших новых видео с каналов из списка.

---

### 📦 Возможности.

* **Сканирование:** Парсинг новых вышедших видео роликов.
* **Сбор данных:** ID, название, описание, ... . Ничего лишнего.
* **Подключение:** Мы используем YouTube API через OAuth 2.0.

---

### ⚙️ Установка

1.  **Клонирование:**
    ```bash
    git clone [https://github.com/mnovouralsk/Youtube_parse_python_bot.git](https://github.com/mnovouralsk/Youtube_parse_python_bot.git)
    cd Youtube_parse_python_bot
    ```
2.  **Изоляция:**
    ```bash
    python -m venv venv
    # Windows
    venv\Scripts\activate
    # Linux/macOS
    source venv/bin/activate
    ```
3.  **Зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
4.  **Доступ:**
    * Получи `client_secret.json` с Google Cloud Console. Это твой ключ.
    * Положи его в корень проекта.

---

### 🚀 Конфигурация

[ файл .env ]

```# Telegram
BOT_TOKEN = "░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░"
MODERATOR_CHAT_ID = [░░░░░░░░░░░░]

# Channels
GROUP_SCI_FI = "░░░░░░░░░░░░░"
GROUP_HORROR = "░░░░░░░░░░░░░"
GROUP_ACTION = "░░░░░░░░░░░░░"

# YouTube API
YOUTUBE_API_KEY = "YOUR_YT_API_KEY"
YOUR_CLIENT_SECRET_FILE = "YOUR_CLIENT_SECRET_FILE.json"
USE_OAUTH = True  # True, если хочешь использовать OAuth
                  # False, если хочешь использовать только API Key

TOKEN_FILE = "data/token.pickle"
CHECK_INTERVAL_HOURS = 1
CHANNELS_JSON = "data/channels.json"
START_DATE = "2025-11-01T00:00:00+00:00"

# JSON файлы
LAST_VIDEO_JSON = "data/last_video_ids.json"
PENDING_POSTS_JSON = "data/pending_posts.json"

# LLM g4f
MAX_RETRIES = 3

# Logging
LOG_FILE = "log/release_tracker.log"
LOG_LEVEL = "DEBUG"
```

### 📂 Структура
```bash
Youtube_parse_bot/
│
├── bot/
│   ├── __init__.py
│   ├── handlers.py          # Обработчики команд и callback
│   ├── keyboards.py         # Inline клавиатуры для модерации
│   ├── bot_main.py          # Основной запуск бота
│   └── moderation.py        # Модерация постов
│── core/
│   ├── __init__.py
│   ├── yt_parser/
│   │   ├── __init__.py
│   │   ├── ytube_parser.py      # Работа с YouTube API
│   │   └── video_storage.py     # Хранение последних videoId и постов в JSON
│   ├── llm/
│   │   ├── __init__.py
│   │   ├── chatgpt.py           # Обработка g4f, генерация постов и жанра
│   │   ├── prompts.py           # Промты для генерации текста и определения жанра
│   └── logger.py                # Настройка логирования
├── config.py             # Настройки (бот токен, API ключи, тайминги)
├── requirements.txt
└── main.py               # Скрипт для запуска бота с перезапуском
```

### ⚠️ Важные моменты

- Для аутентификации используем flow.run_local_server(). Старые методы уже не работают.

### 🔗 Полезные ссылки
[Google YouTube API](https://developers.google.com/youtube/v3)
